[project]
name = "sentarc-agent"
version = "0.1.0"
description = "Agentic loop and session management — Python port of @sentarc-labs/sentarc-agent-core"
requires-python = ">=3.11"
dependencies = [
    "sentarc-ai",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[tool.setuptools]
# sentarc-mono: files live directly in src/, mapped to sentarc_agent package
package-dir = {"sentarc_agent" = "src"}
packages = ["sentarc_agent"]
